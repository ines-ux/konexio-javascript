var arr = [ 'm', 'n', 'o', 'p' ];

var arr2 = arr.map(function(el) {
	return el.toUpperCase();
	// to uppercase changer une minuscule en majuscule
});

console.log(arr2);

/* Exemple d'indentation de code et de mise en place de parenthèses :

function exemple(test) {
    var var1 = 0

    for (var 1 = 0; 1 < 10; 1++) {
        var = test.truc( function { du code } );
        ici du code;
        
    }

    return var;

}

*/
